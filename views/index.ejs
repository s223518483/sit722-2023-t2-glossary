<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= title %></h1>
    <p><%= intro %></p>
    <table>
      <% table_dt.forEach((data) => { %>
      <tr>
        <td><%= data.id %></td>
        <td><%= data.description %></td>
        <td><%= data.reference %></td>
      </tr>
      <%}); %>
    </table>
  </body>
</html>
